# Ship

Stage all changes, commit, and push to remote.

## Instructions

1. Run `git add -A` to stage all changes
2. Run `git status` and `git diff --staged` to see what will be committed
3. Generate a concise commit message based on the changes
4. Run `git commit -m "<message>"` with the generated message
5. Run `git push` to push to remote
